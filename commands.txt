docker exec -it xdb-mongo_my-mongoDB_1 bash
mongo
show dbs
use admin
db.createUser(
  {
    user: "victor",
    pwd: passwordPrompt(), 
    roles: [ { role: "userAdminAnyDatabase", db: "admin" }, "readWriteAnyDatabase" ]
  }
)
use students
db.students.save({firstname:"Victor",lastname:"Wolf"})
db.students.save({firstname:"Max",lastname:"Mustermann"})
db.students.find({firstname:"Max"})